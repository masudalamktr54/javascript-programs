// first method--------------------------
const squareRoot = (num) => {
	console.log(Math.sqrt(num))
}
squareRoot(625)

// second method--------------------------
const squareRoot2 = (a, b, c) => {
	// let num = (a*a) + ((b*b)-(4*a*c)).
	let num = -28
	console.log(num)
	console.log(Math.pow(num, 1/2))
}
squareRoot2(2, 4, 6)